# Deploy Package

This directory will contain UFO deployment and teleoperation code.

Planned components:

- realtime policy runner
- realtime latent `z` server
- keyboard/gamepad/teleop clients
- robot runtime configs
- ONNX policy and backward-encoder loaders

Do not add training dependencies to this branch unless they are needed by deployment.
